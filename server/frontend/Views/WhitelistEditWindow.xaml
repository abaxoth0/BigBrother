<Window x:Class="frontend.Views.WhitelistEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="White"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name Input -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Название: " VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Text="{Binding WhitelistName, UpdateSourceTrigger=PropertyChanged}" Width="250"/>
        </StackPanel>

        <!-- Entries Text Area -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Записи (по одной на строку):" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Grid.Row="1" Text="{Binding EntriesText, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas" Padding="5"
                     BorderBrush="#CCCCCC" BorderThickness="1"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="80" Height="25" Margin="0,0,5,0"
                    Command="{Binding SaveCommand}" IsDefault="True"/>
            <Button Content="Отмена" Width="80" Height="25" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
