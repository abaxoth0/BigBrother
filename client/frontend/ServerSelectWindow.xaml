<Window x:Class="frontend.ServerSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор сервера" Height="350" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="White" ResizeMode="CanResizeWithGrip">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Найденные серверы:" FontWeight="Bold" Margin="0,0,0,5"/>

        <ListBox Grid.Row="1" x:Name="ServersListBox"
                 DisplayMemberPath="DisplayText"
                 SelectionMode="Single"
                 BorderBrush="#CCCCCC"
                 Margin="0,0,0,10">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ConnectButton" Content="Подключиться" Padding="12,6" Margin="0,0,10,0" IsEnabled="False" Click="ConnectButton_Click"/>
            <Button Content="Отмена" Padding="12,6" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
